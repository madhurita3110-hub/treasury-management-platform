server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/treasury
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/treasurydb
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
      ddl-auto: update   # auto-create/update tables
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true